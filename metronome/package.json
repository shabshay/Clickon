{
  "name": "metronome",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:entries && tsc -b && vite build",
    "preview": "vite preview",
    "check:entries": "node scripts/enforce-ts-entry.mjs",
    "lint": "npm run check:entries && tsc -b",
    "tsc": "tsc -b && vite build"
  },
  "dependencies": {
    "pinia": "^2.1.7",
    "vue": "^3.4.0",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.1.0",
    "vite-plugin-pwa": "^0.17.5"
  }
}
